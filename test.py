#! /usr/bin/env python3

import porphyrin.convert as convert

IN=test-in
OUT=test-out

for files in "${IN}":
   file_out = #XXX
   str_in = read(file)
   str_out = convert(str_in)
   file_out = convert()

